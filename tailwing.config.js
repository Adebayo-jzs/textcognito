module.exports = {
  darkMode: 'class', // This is crucial for manual toggling
  theme: {
    extend: {
      fontFamily: {
        // This sets Outfit as the default font for everything
        sans: ['var(--font-outfit)', 'sans-serif'], 
      },
    },
  },
}